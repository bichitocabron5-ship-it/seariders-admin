export type ServiceRow = {
  id: string;
  name: string;
  category: string;
  isActive: boolean;
  requiresPlatform: boolean;
  requiresJetski: boolean;
  requiresMonitor: boolean;
  isLicense: boolean;
};
